---
layout: base.njk
title: "Reflexões, Floriano Silva · Digital Product Builder"
description: "Artigos e reflexões sobre design, growth, comportamento, conversão, IA e experiência orientada a dados."
activeNav: reflexoes
schemaType: Blog
---

  <!-- ================================================
       REFLEXÕES , LISTAGEM
       ================================================ -->
  <main class="reflections section page-top" id="reflexoes">
    <div class="container">

      <h1 class="page-title" data-animate>Reflexões:</h1>

      <div class="reflections__grid">

        {% if reflexoes.items.length %}
          {% for post in reflexoes.items %}
            {% set delay = (loop.index0 % 3) + 1 %}
            <article class="post-card" data-animate data-delay="{{ delay }}">
              <a href="{{ post.url }}" class="post-card__link">
                <div class="post-card__thumb">
                  {% if post.thumbnail %}
                    <img
                      src="{{ post.thumbnail }}"
                      alt="{{ post.title }}"
                      loading="lazy"
                    >
                  {% else %}
                    <div style="width:100%;height:100%;background:rgba(255,255,255,.06);"></div>
                  {% endif %}
                  <div class="post-card__thumb-gradient"></div>
                </div>
                <div class="post-card__body">
                  <h2 class="post-card__title">{{ post.title }}</h2>
                  {% if post.excerpt %}
                    <p class="post-card__excerpt">{{ post.excerpt }}</p>
                  {% endif %}
                  <span class="post-card__cta">Leia mais »</span>
                </div>
                {% if post.dateDisplay %}
                  <time class="post-card__date" datetime="{{ post.dateAttr }}">{{ post.dateDisplay }}</time>
                {% endif %}
              </a>
            </article>
          {% endfor %}
        {% else %}
          <p style="color: rgba(230,230,230,.5); grid-column: 1 / -1;">
            Nenhum artigo encontrado. Confira diretamente no
        <a href="https://medium.com/@floriano-des" target="_blank" rel="noopener noreferrer" style="color: inherit; text-decoration: underline;">Medium</a>.
          </p>
        {% endif %}

      </div>
    </div>
  </main>
